apiVersion: v1
kind: ConfigMap
metadata:
  name: kong-config
  namespace: system
data:
  # 将你的 kong.yaml 内容粘贴在这里
  kong.yaml: |
    _format_version: "1.1"
    services: []
  KONG_ADMIN_GUI_API_URL: "http://test.local.lexmount.net:8081/api"